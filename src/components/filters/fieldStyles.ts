// Shared input styling so every filter control (table + globe) looks identical.
export const fieldClass =
  'w-full rounded-lg border border-border bg-surface px-3 py-2 text-sm text-fg focus:border-cyan focus:outline-none focus:ring-2 focus:ring-cyan/40'

export const labelClass = 'block text-xs uppercase tracking-widest text-muted'
